"use client";

import { useSearchParams } from "next/navigation";
import React from "react";

const ProductConfirmation = ({}: {}) => {
  const searchParams = useSearchParams();
  return <div>ProductConfirmation</div>;
};

export default ProductConfirmation;
